// use uuid::Uuid;

pub fn on_save_zknote(noteid: &uuid::Uuid, script: String) {
  std::thread::spawn(move || {i
    // maybe use duct.rs?
    //
    // oar xshell for a cross platform shell-like language.

    
    std::process::Command::new("/bin/sh"});
}
